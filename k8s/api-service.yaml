apiVersion: v1
kind: Service
metadata:
  name: truestake-api
  labels:
    app: truestake-api
spec:
  type: ClusterIP
  selector:
    app: truestake-api
  ports:
    - name: http
      port: 3000
      targetPort: 3000
      protocol: TCP
